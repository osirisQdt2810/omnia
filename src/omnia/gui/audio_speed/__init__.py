"""Web assets for the audio-speed plugin (``web/speed.js``).

A package so :func:`omnia.gui.assets.read_asset` can locate the script relative to this file,
the same way every other plugin's reviewer JS is found.
"""
