"""The Sync panel: an Omnia-level dialog, not a plugin's.

It belongs to no feature plugin — the settings grid reaches it through an action tile rather
than a category — so it lives here beside the settings page rather than under ``plugins/``.
"""
