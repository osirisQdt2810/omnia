"""Omnia core — the shared seams every feature plugin sits on.

This package is feature-agnostic: it must never import from ``omnia.plugins``. See
``.claude/CLAUDE.md`` (the four shared seams) and ``.claude/CONVENTIONS.md`` Part 2.
"""
